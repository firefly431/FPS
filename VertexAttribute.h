#pragma once

// vim: set ft=cpp:

// these are the attributes indices used in the shader
enum VertexAttribute {
    ATTRIBUTE_NONE,
    ATTRIBUTE_POSITION,
    ATTRIBUTE_NORMAL,
    ATTRIBUTE_TEXCOORD,
    ATTRIBUTE_NUM
};

